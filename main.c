/**
 * O programa principal inicia a execução chamando a função menu() do módulo "agenda.h".
 *
 * @note Inclui o cabeçalho "agenda.h" para ter acesso às definições e funcionalidades do programa.
 * @note Chama a função menu() que é responsável por iniciar a interação do usuário com o sistema.
 * @note Retorna 0 para indicar que a execução do programa foi concluída sem erros.
 */

#include "agenda.h"

int main()
{
    menu();  // Chama a função menu() para iniciar a interação com o usuário.
    return 0;  // Retorna 0 para indicar que a execução foi concluída sem erros.
}
